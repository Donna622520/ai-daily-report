# AI 日报 V4 - 经验沉淀文档

> 问题视角：记录成功经验、失败教训、验证经验、在哪里错了就停止

---

## 📋 目录

1. [版本演进历程](#版本演进历程)
2. [关键问题与解决方案](#关键问题与解决方案)
3. [验证经验总结](#验证经验总结)
4. [失败教训记录](#失败教训记录)
5. [资源搜索经验](#资源搜索经验)

---

## 版本演进历程

### V1 版本（初始版）
- **特点**：简单 HTML，无固定样式
- **问题**：样式不统一，每次生成效果不同
- **经验**：需要固定样式模板

### V2 版本（尝试版）
- **特点**：尝试多种样式
- **问题**：每次自由发挥，UI 效果不一致
- **经验**：需要配置化系统

### V3 版本（配置化尝试）
- **特点**：使用 CSS 变量 + 配置 YAML
- **问题**：浏览器缓存导致样式不显示
- **经验**：需要内联 CSS 或直接复制最终版样式

### V4 版本（标准版）
- **特点**：固定样式，卡片式布局，800px 宽度
- **状态**：✅ 已稳定，可长期使用
- **经验**：直接复制最终版 CSS 最可靠

---
## 关键问题与解决方案

### 问题 1：UI 样式每次都不一样

**现象**：
- V1、V2、V3 版本的 UI 效果每次都不同
- 用户无法预测报告长什么样

**原因分析**：
- 没有统一的 CSS 配置文件
- 每次生成都是手写 HTML 中的 `<style>` 标签
- 没有版本化的 UI 模板

**解决方案**：
1. **方案 A**：创建独立的 CSS 文件（未采用，浏览器缓存问题）
2. **方案 B**：配置化主题（已采用，但最终发现 CSS 变量仍有问题）
3. **方案 C**：直接复制最终版 CSS（✅ 最终采用）

**实施步骤**：
1. 找到用户满意的最终版报告
2. 提取其中的 `<style>` 标签内容
3. 保存到 `styles/v4_standard.css`
4. 生成脚本直接引用该 CSS

**经验教训**：
- 不要过度设计配置系统
- 直接复制最终版最可靠
- 避免使用 CSS 变量（可能导致渲染问题）

---

### 问题 2：9 个板块出现空白

**现象**：
- 某些板块（如"海外最新动态"、"GitHub 热门 AI 项目"）没有新闻
- 报告看起来不完整

**原因分析**：
- 只采集了国内新闻源（量子位、机器之心、36Kr）
- 没有采集海外新闻源（Hacker News、GitHub 等）
- 新闻分类脚本未覆盖所有板块

**解决方案**：
1. 扩展新闻源列表，添加海外来源
2. 确保每个板块至少有 1 条新闻
3. 生成前验证板块数量

**实施步骤**：
```bash
# 1. 添加海外新闻源
sources:
  - https://news.ycombinator.com/
  - https://huggingface.co/blog
  - https://github.com/trending
  - https://www.producthunt.com/topics/ai

# 2. 验证板块数量
python3 -c "import json; data=json.load(open('/tmp/news_final.json')); print(f'板块数：{len(data)}')"
```

**经验教训**：
- 必须覆盖国内外新闻源
- 生成前必须验证所有板块都有数据
- 不要假设某个板块一定有新闻

---

### 问题 3：历史数据混入

**现象**：
- 报告中出现了过去日期的新闻
- 违反了"只采集当天新闻"的规范

**原因分析**：
- 爬虫未正确过滤日期
- 新闻源页面包含历史新闻
- 未严格验证每条新闻的日期

**解决方案**：
1. 在采集时严格过滤日期
2. 生成前检查每条新闻的日期字段
3. 使用 grep 验证报告中的日期一致性

**实施步骤**：
```bash
# 验证日期一致性
grep -o "2026-04-26" reports/v4_2026-04-26_V4_标准版.html | wc -l
# 应该等于新闻总数 × 2（日期字段出现 2 次）
```

**经验教训**：
- 必须在采集阶段就过滤日期
- 生成后必须验证日期一致性
- 宁可少一条新闻，也不能混入历史数据

---
## 验证经验总结

### 验证检查清单

每次生成报告后，必须执行以下验证：

#### 1. 数据完整性验证
```bash
# 检查新闻数据文件
ls -l /tmp/news_final.json

# 检查板块数量
python3 -c "import json; data=json.load(open('/tmp/news_final.json')); print(f'板块数：{len(data)}')"
# 应该等于 9

# 检查总新闻数
python3 -c "import json; data=json.load(open('/tmp/news_final.json')); print(f'总新闻数：{sum(len(v) for v in data.values())}')"
# 应该 ≥ 10
```

**验收标准**：
- ✅ 板块数 = 9
- ✅ 总新闻数 ≥ 10
- ✅ 每个板块至少有 1 条新闻

#### 2. 数据准确性验证
```bash
# 检查日期一致性
grep -o "2026-04-26" reports/v4_2026-04-26_V4_标准版.html | wc -l
# 应该等于 新闻总数 × 2（日期字段在标题和元数据中各出现 1 次）

# 检查新闻项数量
grep -c "class=\"news-item\"" reports/v4_2026-04-26_V4_标准版.html
# 应该等于新闻总数
```

**验收标准**：
- ✅ 所有新闻日期都是 2026-04-26
- ✅ 新闻项数量 = 总新闻数
- ✅ 无历史日期混入

#### 3. 样式规范性验证
```bash
# 检查 CSS 文件是否存在
ls -l styles/v4_standard.css

# 检查报告是否引用了 CSS
grep -c "styles/v4_standard.css" reports/v4_2026-04-26_V4_标准版.html
# 应该等于 1（内联 CSS 则检查 <style> 标签）
```

**验收标准**：
- ✅ CSS 文件存在
- ✅ 报告包含 CSS 样式
- ✅ 样式内容完整（> 2000 字符）

#### 4. 文件管理验证
```bash
# 检查报告文件
ls -l reports/v4_2026-04-26_V4_标准版.html

# 检查备份文件
ls -l reports/archive/

# 检查临时文件（应该清理）
ls -l /tmp/news_*.json
```

**验收标准**：
- ✅ 报告文件存在且 > 10KB
- ✅ 有历史版本备份
- ✅ 临时文件已清理（或计划清理）

---

### 验证失败的处理流程

如果任何验证失败：

1. **数据完整性失败**：
   - 检查新闻源是否可访问
   - 重新运行爬虫脚本
   - 如果仍然失败，手动添加新闻

2. **数据准确性失败**：
   - 检查每条新闻的日期字段
   - 删除历史日期新闻
   - 重新生成报告

3. **样式规范性失败**：
   - 检查 CSS 文件内容
   - 重新复制最终版 CSS
   - 重新生成报告

4. **文件管理失败**：
   - 手动创建备份
   - 清理临时文件
   - 记录清理日志

---
## 失败教训记录

### 教训 6：Git 推送认证失败

**场景**：
直接 `git push` 时遇到认证问题（"could not read Username"）。

**错误行为**：
- 尝试网页认证（失败）
- 配置 SSH Key（复杂且不需要）
- 禁用 credential helper（临时方案）
- 多次尝试不同方法

**问题发现**：
- 浪费时间尝试多种方法
- 每次都要重新摸索流程
- 没有标准化的解决方案

**正确做法**：
1. 使用 `github-push-success` 技能（已固化）
2. 按标准流程执行：克隆 → 复制 → 提交 → Token URL → 推送
3. 不再尝试其他方法

**实施步骤**：
```bash
# 调用 github-push-success 技能
# 或直接执行标准流程：
temp_dir="/tmp/ai-daily-report-temp"
rm -rf "$temp_dir"
mkdir -p "$temp_dir"
cd "$temp_dir"
git clone https://github.com/Donna622520/ai-daily-report.git
cp -r /home/donna/Hermes/workspace/projects/ai-daily-report-V4-0426/* "$temp_dir/ai-daily-report/"
cd "$temp_dir/ai-daily-report"
git config user.email "donna@example.com"
git config user.name "Donna"
git add .
git commit -m "feat: 描述本次提交"
git remote set-url origin https://Donna622520:$GITHUB_TOKEN@github.com/Donna622520/ai-daily-report.git
GIT_TERMINAL_PROMPT=0 git push origin main
rm -rf "$temp_dir"
```

**改进措施**：
- ✅ 已创建 `github-push-success` 技能（`/home/donna/.hermes/skills/devops/github-push-success/SKILL.md`）
- ✅ 在资源库.md 中记录技能入口
- ✅ 遇到问题先搜索技能，不再盲目尝试

**相关技能**：
- 📚 [github-push-success](file:///home/donna/.hermes/skills/devops/github-push-success/SKILL.md) - GitHub 推送标准流程

---

### 教训 1：不要忽略历史经验

**场景**：
V4 项目启动时，没有检查 V1 项目中已有的新闻源配置和经验沉淀。

**错误行为**：
- 重新探索新闻源（量子位、机器之心、36Kr）
- 重复验证已经验证过的 URL
- 浪费时间重新配置

**正确做法**：
1. 遇到问题时，先搜索历史经验文档（EXPERIENCE.md）
2. 检查旧版本项目（V1）的配置文件
3. 直接继承已有的验证结果

**改进措施**：
- 在 SKILL.md 中明确"先检查历史经验"的步骤
- 在资源库.md 中记录已验证的资源链接
- 遇到问题时，先搜索再行动

---

### 教训 2：不要过度设计配置系统

**场景**：
尝试用 CSS 变量 + YAML 配置的方式实现 UI 配置化。

**错误行为**：
- 创建了 config-ui.yaml 配置文件
- 使用 CSS 变量（--color-primary 等）
- 期望通过修改 YAML 就能调整样式

**问题发现**：
- 浏览器缓存导致样式不显示
- CSS 变量在某些情况下渲染异常
- 用户看不到预期的样式效果

**正确做法**：
1. 直接复制最终版 CSS 到独立文件
2. 生成脚本直接引用该 CSS
3. 不再使用配置系统

**改进措施**：
- 简单直接的方法优于复杂配置
- 优先保证效果，再考虑灵活性
- 如果必须配置，确保兼容性

---

### 教训 3：不要假设板块一定有新闻

**场景**：
认为某些板块（如"GitHub 热门 AI 项目"）一定有新闻。

**错误行为**：
- 只采集国内新闻源
- 没有采集 GitHub、Product Hunt 等来源
- 生成报告时发现板块空白

**问题发现**：
- 5 个板块为空（海外、融资、政策、GitHub、Product Hunt）
- 报告看起来不完整
- 用户不满意

**正确做法**：
1. 采集前明确所有需要的新闻源
2. 确保每个板块都有对应的新闻源
3. 生成前验证每个板块都有数据

**改进措施**：
- 在 config.yaml 中明确列出所有新闻源
- 生成脚本中验证板块数量
- 空板块必须有明确说明（"今日暂无新闻"）

---

### 教训 4：不要混入历史数据

**场景**：
爬虫采集的新闻中包含过去日期的内容。

**错误行为**：
- 未严格过滤日期
- 直接使用爬虫返回的所有新闻
- 未验证每条新闻的日期

**问题发现**：
- 报告中出现了 2026-04-25 的新闻
- 违反了"只采集当天新闻"的规范
- 报告可信度下降

**正确做法**：
1. 爬虫采集时严格过滤日期
2. 生成前检查每条新闻的日期字段
3. 生成后验证报告中的日期一致性

**改进措施**：
- 在爬虫脚本中添加日期过滤逻辑
- 生成脚本中添加日期验证步骤
- 使用 grep 命令验证日期一致性

---

### 教训 5：不要忽略浏览器缓存

**场景**：
修改了 CSS 文件，但浏览器仍然显示旧样式。

**错误行为**：
- 直接打开报告，未强制刷新
- 未检查浏览器缓存设置
- 误以为样式未生效

**问题发现**：
- CSS 文件已更新，但浏览器显示旧样式
- 用户反馈"样式没变"
- 浪费时间排查其他问题

**正确做法**：
1. 修改 CSS 后，强制刷新浏览器（Ctrl+F5）
2. 或者使用无痕模式打开
3. 或者清除浏览器缓存

**改进措施**：
- 在 README.md 中添加"浏览器缓存"说明
- 生成报告时添加时间戳参数（?v=timestamp）
- 优先使用内联 CSS（避免缓存问题）

---

## 经验沉淀总结

### 核心原则

1. **先检查历史经验**：遇到问题先搜索 EXPERIENCE.md 和旧版本项目
2. **简单直接优先**：直接复制最终版优于复杂配置系统
3. **验证必须彻底**：数据完整性、准确性、样式规范性都要验证
4. **不要假设**：每个板块都可能为空，必须验证
5. **日期严格过滤**：只采集当天新闻，混入历史数据是严重错误

### 最佳实践

1. **生成前验证**：检查新闻数据文件、板块数量、新闻总数
2. **生成后验证**：检查报告文件、日期一致性、样式规范性
3. **备份历史版本**：每次生成前备份旧报告
4. **清理临时文件**：定期清理 /tmp 目录
5. **记录经验教训**：及时更新 EXPERIENCE.md

---

**最后更新**：2026-04-26  
**版本**：V4.0
