# AI 日报项目经验总结

## 📖 项目演进

### V1 → V2 → V3 演进记录

**V1 (初始版本)**:
- 基础爬虫框架
- 简单 HTML 生成
- 问题：爬虫反爬失败，输出截断

**V2 (优化版本)**:
- 添加来源优先级标识
- 实现分段生成策略
- 问题：爬虫仍无法绕过 Cloudflare

**V3 (当前版本)**:
- 6 个板块结构
- 手动采集 + 交叉验证
- 未找到板块明确标注
- 飞书超链接格式优化

---

## ✅ 成功经验

### 1. 分段生成策略（大文档生成）

**问题**: 一次性生成完整 HTML 并输出到终端会导致输出截断

**错误做法**:
```bash
# ❌ 错误：直接输出完整 HTML
echo "<html>...</html>" > report.html

# ❌ 错误：在内存中构建完整文档
python -c "print(full_html)" > report.html
```

**正确做法**:
```bash
# ✅ 正确：分段追加到文件
cat > /tmp/daily_header.html << 'EOF'
<!DOCTYPE html>
...
EOF

cat >> /tmp/daily_header.html << 'EOF'
... 内容 ...
EOF

mv /tmp/daily_header.html archive/2026-04/v3_2026-04-24.html
```

**关键要点**:
- 使用 `cat >> file.html` 分段追加
- 所有操作在文件层面完成
- 不输出完整 HTML 到终端
- 飞书通知只包含链接和摘要

**适用场景**:
- 任何大内容生成任务
- 输出超过 3000 字符的内容
- 需要避免截断的场景

---

### 2. 飞书通知规范

**超链接格式**:
```markdown
✅ 正确：[显示文本](URL)
❌ 错误：https://example.com（纯文本）
```

**通知模板**:
```markdown
📰 **AI 日报 - {date}**

🔗 **查看日报**: [完整链接](完整链接)

📚 **历史归档**: [归档页面](归档链接)

---
**今日重点**:
1. 重点新闻 1
2. 重点新闻 2
```

**关键要点**:
- 必须使用 Markdown 格式
- 纯文本 URL 会显示为黑色字符串
- 使用 `send_message` 工具
- target 格式：`feishu:chat_id`

---

### 3. GitHub 认证优化

**问题**: `GH_TOKEN` 为空导致远程 URL 包含 `x-access-token:@`

**错误做法**:
```bash
# ❌ 错误：硬编码 token 到 URL
git remote set-url origin https://x-access-token:TOKEN@github.com/...
```

**正确做法**:
```bash
# ✅ 正确：使用 gh CLI
gh auth login
gh repo create ...

# 或使用标准 HTTPS URL
git remote set-url origin https://github.com/用户名/仓库.git
```

**关键要点**:
- 不使用 `x-access-token:` 格式
- 使用 `gh` CLI 进行认证
- 避免空 token 导致认证失败

---

### 4. 未找到板块处理规范

**问题**: 某板块当天无新闻时，如何处理？

**错误做法**:
```markdown
❌ 留空不写
❌ 模糊处理
❌ 提供猜测性内容
```

**正确做法**:
```markdown
### 📊 GitHub Trending AI
❌ 未找到 GitHub Trending AI 新闻
原因：当天无相关新闻或爬虫未能成功抓取
建议：手动访问 https://github.com/trending 查看
```

**关键要点**:
- 必须明确标注"未找到"
- 说明原因
- 提供手动访问链接
- 建议替代方案

---

## ❌ 失败教训

### 1. 爬虫反爬问题

**问题**: Product Hunt 有 Cloudflare 防护，TechCrunch 超时

**原因分析**:
- 爬虫无法绕过 Cloudflare 反爬机制
- 目标网站有严格的请求频率限制
- User-Agent 更新后仍失败

**解决方案**:
- 放弃自动爬虫，改用手动采集
- 手动访问目标网站采集数据
- 聚焦于数据质量和准确性

**经验总结**:
- 不要过度依赖自动爬虫
- 对于反爬严格的网站，手动采集更可靠
- 数据质量 > 自动化程度

---

### 2. 输出截断问题

**问题**: 一次性生成完整 HTML 导致输出截断

**原因分析**:
- 终端输出限制
- 内存中构建完整文档
- 没有分段处理

**解决方案**:
- 使用 `cat >> file.html` 分段写入
- 所有操作在文件层面完成
- 避免输出完整内容到终端

**经验总结**:
- 大内容生成必须分段处理
- 文件操作优于内存操作
- 输出截断是常见问题，需提前预防

---

### 3. 文档重复问题

**问题**: README.md 和 SKILL.md 内容重复，维护成本高

**原因分析**:
- 职责边界不清晰
- 没有明确文档规范
- 经验沉淀分散

**解决方案**:
- README.md: 只保留项目核心信息
- EXPERIENCE.md: 专门记录经验教训
- SKILL.md: 只保留通用流程

**经验总结**:
- 文档职责必须明确
- 避免内容重复
- 及时清理冗余文档

---

## 🎯 最佳实践

### 1. 任务交付流程

**每次任务完成后，执行 3 步流程**:

1. **列出交付结果路径**
   - 所有文件路径已记录
   - 关键文件已确认存在

2. **确认经验沉淀**
   - 判断是否需要沉淀经验
   - 创建/更新技能文档
   - 更新项目文档（如需要）

3. **GitHub 已推送**
   - `git status` 确认无遗漏
   - `git add -A` 添加所有变更
   - `git commit` 提交变更
   - `git push` 推送到远程

**相关技能**: `delivery-pipeline`

---

### 2. 经验沉淀规范

**需要沉淀的场景**:
- ✅ 发现新流程
- ✅ 解决复杂问题
- ✅ 优化现有方案
- ✅ 创建新工具

**不需要沉淀的场景**:
- ❌ 简单查询
- ❌ 一次性操作
- ❌ 已知方案

**沉淀方式**:
1. **创建新技能** - 新流程、新工具、新策略
2. **更新现有技能** - 优化现有流程、补充遗漏步骤
3. **更新项目文档** - 项目特定经验

---

### 3. 数据质量优先

**原则**:
- 数据来源必须是"官方可验证的真实消息"
- 排除传闻谣言
- 交叉验证确保准确性

**数据来源优先级**:
1. **一级优先**: 官方博客、官方 API
2. **二级优先**: 权威新闻网站
3. **三级优先**: 社区聚合

---

## 📝 常见问题

### Q1: 为什么爬虫失败了？
**A**: Product Hunt 有 Cloudflare 防护，TechCrunch 超时。解决方案：改用手动采集。

### Q2: 如何避免输出截断？
**A**: 使用 `cat >> file.html` 分段写入，不输出完整内容到终端。

### Q3: 飞书通知为什么链接不显示？
**A**: 必须使用 Markdown 格式 `[显示文本](URL)`，纯文本 URL 会显示为黑色字符串。

### Q4: GitHub 推送失败怎么办？
**A**: 检查 `GH_TOKEN` 是否为空，使用 `gh` CLI 进行认证，避免硬编码 token。

### Q5: 某板块没有新闻怎么办？
**A**: 必须明确标注"未找到"，说明原因，提供手动访问链接。

---

## 🔗 相关资源

- **交付流程**: [delivery-pipeline](~/.hermes/skills/devops/delivery-pipeline/SKILL.md)
- **大文档策略**: [large-document-writing](~/.hermes/skills/devops/large-document-writing/SKILL.md)
- **项目 README**: [README.md](README.md)

---

*最后更新：2026-04-24*
*创建原因：整合项目经验，避免文档重复，便于后续维护*
